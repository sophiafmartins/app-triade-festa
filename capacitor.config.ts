import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-triade-festa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
